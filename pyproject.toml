[project]
name = "nlp-assignment-1"
version = "0.1.0"
description = "NLP Assignment 1"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "nltk>=3.8.1",
    "numpy>=1.26.2",
    "pandas>=2.1.3",
    "scikit-learn>=1.3.2",
    "streamlit>=1.28.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
